name: Auto update upstream

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 */6 * * *'  # 每6小时同步一次

jobs:
  auto-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Fetch upstream
        run: |
          git remote add upstream https://github.com/Gnuyoah/Thread.git
          git fetch upstream
          
      - name: Sync with upstream
        run: |
          git fetch upstream
          git checkout main
          git merge upstream/main --no-edit
          git push

    
